# Frontend environment variables
# Vite exposes variables prefixed with VITE_ to the client

# API base URL (leave empty to use Vite proxy in dev)
VITE_API_URL=

# App name shown in UI
# VITE_APP_NAME=Transcribe
